/**
 * Smartsupp chat widget loader.
 *
 * Placeholder component — not mounted in the layout yet. When ready,
 * drop <Smartsupp /> into the root layout body and set
 * NEXT_PUBLIC_SMARTSUPP_KEY in your env.
 */
export function Smartsupp() {
  return null;
}
